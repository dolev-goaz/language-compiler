
int_16 n = 5;
if (n == 5) {
    n = n - 1;
}
int_64 total = factorial(n);

exit(total);

func int_16 factorial(int_16 y) {
    int_64 x;
    if (y){
        return y * factorial(y - 1);
    } else {
        return 1;
    }
}