
int_16 n = 5;

// TODO: don't allow assigning of void function call
int_16 h = try_exit(10);
exit(h);
try_exit(15);
try_exit(20);

func void try_exit(int_16 x) {
    if (x <= 15) {
        return;
    }
    
    exit(x);
}