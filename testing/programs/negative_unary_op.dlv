int_64 x = 5;
int_64 y = -x;
int_64 z = -(x + 2);
int_64 w = -getValue();

if (-x == -5) {
    if (y == -5) {
        if (z == -7) {
            if (w == -10) {
                exit(5);
            }
        }
    }
}

exit(0);

func int_64 getValue() {
    return 10;
}